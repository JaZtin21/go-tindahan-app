// Deprecated barrel — all restaurant GraphQL operations now live in
// `src/api/queries/graphql/restaurant/`. This file exists only so existing
// `~/api/graphql` imports keep resolving.
export * from '~/api/queries/graphql/restaurant';
